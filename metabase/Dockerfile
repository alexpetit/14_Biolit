# Metabase pour Clever Cloud (app web Docker, dataviz).
# Déployée via CC_DOCKERFILE=metabase/Dockerfile.
# Version alignée avec le dump Coolify à restaurer.
FROM metabase/metabase:v0.60.1.3

# Clever Cloud expose les apps Docker sur 8080 par défaut.
ENV MB_JETTY_PORT=8080
