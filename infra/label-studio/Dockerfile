# Label Studio pour Clever Cloud (app web Docker, séparée de l'ingestion).
# Déployée via la variable d'env CC_DOCKERFILE=infra/label-studio/Dockerfile.
# L'image officielle démarre Label Studio sur le port 8080 (défaut Clever Cloud).
FROM heartexlabs/label-studio:1.13.1
