# Metabase pour Clever Cloud (app web Docker, dataviz).
# Déployée via CC_DOCKERFILE=infra/metabase/Dockerfile.
# Metabase écoute sur MB_JETTY_PORT (fixé à 8080 = port par défaut Clever Cloud).
FROM metabase/metabase:v0.50.32
